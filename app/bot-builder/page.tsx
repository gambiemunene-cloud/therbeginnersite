'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import { Bot, CheckCircle2, ChevronLeft, Play, Save, ShieldCheck, SlidersHorizontal } from 'lucide-react';
import { useRouter } from 'next/navigation';
import { Header } from '@/components/custom/header';
import { Footer } from '@/components/custom/footer';
import { ThemeToggle } from '@/components/custom/theme-toggle';
import { useLogoSrc } from '@/components/custom/logo-src-provider';
import { useAppTranslations } from '@/components/custom/i18n-provider';
import { useDerivWSContext } from '@/components/custom/deriv-ws-provider';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Badge } from '@/components/ui/badge';

export default function BotBuilderPage() {
  const router = useRouter(); const logoSrc = useLogoSrc(); const { localize } = useAppTranslations();
  const { auth } = useDerivWSContext(); const { authState, accounts, activeAccount, login, signUp, logout, switchAccount } = auth;
  const [name, setName] = useState('Least Digit Scanner'); const [market, setMarket] = useState('R_100'); const [stake, setStake] = useState('1'); const [windowSize, setWindowSize] = useState('100'); const [saved, setSaved] = useState(false);
  useEffect(() => { if (authState === 'unauthenticated' || authState === 'error') router.replace('/'); }, [authState, router]);
  if (authState !== 'authenticated') return <main className="flex min-h-dvh items-center justify-center bg-background"><div className="size-8 animate-spin rounded-full border-2 border-primary border-t-transparent" /></main>;
  return <main className="min-h-dvh bg-background pb-12"><Header authState={authState} accounts={accounts} activeAccount={activeAccount} onLogin={login} onSignUp={signUp} onLogout={logout} onSwitchAccount={switchAccount} logoSrc={logoSrc} actions={<ThemeToggle />} /><div className="h-[76px]" /><div className="mx-auto flex max-w-6xl flex-col gap-6 px-4 py-6"><Link href="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ChevronLeft /> Back to trading</Link><div className="flex items-center justify-between gap-4"><div><div className="flex items-center gap-3"><div className="rounded-xl bg-primary/10 p-3 text-primary"><Bot /></div><div><h2 className="text-2xl font-semibold">Bot builder</h2><p className="text-sm text-muted-foreground">Compose a guarded digit strategy before loading it.</p></div></div></div><Badge variant="secondary">Draft strategy</Badge></div><div className="grid gap-6 lg:grid-cols-[1.2fr_0.8fr]"><Card><CardHeader><CardTitle>Strategy settings</CardTitle><CardDescription>Define the market scanner and trade rules.</CardDescription></CardHeader><CardContent className="flex flex-col gap-5"><div className="flex flex-col gap-2"><Label htmlFor="bot-name">Bot name</Label><Input id="bot-name" value={name} onChange={(e) => setName(e.target.value)} /></div><div className="grid gap-4 sm:grid-cols-2"><div className="flex flex-col gap-2"><Label>Market</Label><Select value={market} onValueChange={setMarket}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="R_100">Volatility 100 Index</SelectItem><SelectItem value="R_50">Volatility 50 Index</SelectItem><SelectItem value="1HZ100V">Volatility 100 (1s)</SelectItem></SelectContent></Select></div><div className="flex flex-col gap-2"><Label htmlFor="window">Rolling tick window</Label><Input id="window" type="number" min="10" max="1000" value={windowSize} onChange={(e) => setWindowSize(e.target.value)} /></div></div><div className="rounded-xl border bg-muted/20 p-4"><div className="mb-3 flex items-center gap-2 font-medium"><SlidersHorizontal /> Trigger rules</div><div className="grid gap-3 text-sm sm:grid-cols-2"><div className="rounded-lg border bg-background p-3"><p className="font-medium">Differs</p><p className="text-xs text-muted-foreground">Trade the least appearing digit.</p></div><div className="rounded-lg border bg-background p-3"><p className="font-medium">Over 1</p><p className="text-xs text-muted-foreground">Enable when digits 0 and 1 are below 10%.</p></div></div></div><div className="flex flex-col gap-2"><Label htmlFor="stake">Stake per trade</Label><Input id="stake" type="number" min="0.35" step="0.01" value={stake} onChange={(e) => setStake(e.target.value)} /></div><div className="flex flex-wrap gap-3"><Button onClick={() => setSaved(true)}><Save data-icon="inline-start" /> {saved ? 'Saved' : 'Save bot'}</Button><Button variant="outline" onClick={() => router.push('/load-bot')}><Play data-icon="inline-start" /> Save and load</Button></div></CardContent></Card><Card><CardHeader><CardTitle>Safety checklist</CardTitle><CardDescription>Every bot starts in observation mode.</CardDescription></CardHeader><CardContent className="flex flex-col gap-4"><div className="flex gap-3"><ShieldCheck className="text-primary" /><div><p className="font-medium">Manual arming required</p><p className="text-sm text-muted-foreground">No trades run until you explicitly arm the bot.</p></div></div>{['Rolling window configured','Stake validation enabled','Kill switch available'].map((item) => <div key={item} className="flex items-center gap-2 text-sm"><CheckCircle2 className="text-emerald-500" />{item}</div>)}<div className="mt-2 rounded-lg bg-muted/40 p-4 text-sm"><p className="font-medium">{name}</p><p className="mt-1 text-muted-foreground">{market} · {windowSize} ticks · {stake} USD stake</p></div></CardContent></Card></div></div><Footer /></main>;
}
